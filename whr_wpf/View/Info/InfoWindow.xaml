<Window x:Class="whr_wpf.View.Info.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:whr_wpf.View.Info"
        mc:Ignorable="d"
        Title="情報" Height="450" Width="200" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="ToolWindow"
        Background="#006699">
    <Grid>
        <StackPanel>
            <Label Content="情報" Foreground="White" HorizontalContentAlignment="Center" FontSize="24" />
            <TextBlock  Text="{Binding InfoStr}" FontSize="14" Foreground="White"/>
            <Button Content="閉じる" Command="{Binding Close}"/>
        </StackPanel>
    </Grid>
</Window>
